{
    "command": "migrate",
    "description": "migrate models",
    "usage": "brewery migrate --options",
    "sub-commands": {
        "subcommand1": {
            "description": "subcommand1 description",
            "usage": "brewery migrate:subcommand1 --options",
            "options": {
                "--exampleOption": {
                    "description": "example option description"
                },
                "--exampleOption2": {
                    "description": "example option description"
                },
                "--exampleOption3": {
                    "description": "example option description"
                }
            },
            "defaultOptions": ["--exampleOption", "exampleOption1"],
        }
    },
    "options": {
        "--exampleOption": {
            "description": "example option description"
        },
        "--exampleOption2": {
            "description": "example option description"
        },
        "--exampleOption3": {
            "description": "example option description"
        }
    },
    "defaultOptions": ["--exampleOption", "exampleOption1"],
    "execPath": "./actions/migrate/"
}